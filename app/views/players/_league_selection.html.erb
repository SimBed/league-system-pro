<%= form_with(url: league_filter_player_path(@player), method: "get", class: "form-class", data: {turbo_frame: "matches_table"}) do |form| %>  
   <%= form.select :player_league_id,
                   options_for_select(@league_options, session.dig(:player_league_ids, @player.id.to_s)),
                   {},
                   { class: 'sort', onchange: 'this.form.requestSubmit()' } %> 
<% end %>