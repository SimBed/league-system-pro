<div class="container px-1">
  <% content_for :title, "Leagues" %>
  <h1 class='h5 mb-2'>Leagues 
    <%= link_to image_tag('add.png', class: "button_icon"), new_league_path, data: { turbo_frame: dom_id(League.new)}, alt: 'New League' %>
  </h1>
  <%= turbo_frame_tag League.new %>
  <%= turbo_frame_tag 'leagues' do %>
    <%= render 'leagues_table' %>
  <% end %>
</div>
